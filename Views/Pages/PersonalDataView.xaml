<Page x:Class="TestWPF.Views.Pages.PersonalDataView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestWPF.Views.Pages"
      mc:Ignorable="d" 
      xmlns:vm="clr-namespace:TestWPF.ViewModes"
      Title="Basic" Height="700" Width="1100"
      Background="Transparent">


    <Border CornerRadius="10"
            BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                <GradientStop Color="#79CBEE"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="500"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
                <RowDefinition Height="140"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="1"  Height="300" Margin="100 0 0 0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="White"/>
                        <GradientStop Color="WhiteSmoke"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="80"/>
                </Border.Effect>
            </Border>

            <Border Grid.Column="1" Grid.Row="1" CornerRadius="20" Margin="0 0 120 0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="#0000FF"/>
                        <GradientStop Color="#4169E1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="60"/>
                </Border.Effect>
               
                    <Border Background="White" Margin="80" CornerRadius="80">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="60"/>
                        </Border.Effect>

                    <StackPanel Margin="0 9">
                        <StackPanel Orientation="Horizontal" Width="250">
                            <TextBlock Text="Выберите категорию" VerticalAlignment="Center" Margin="3 0" FontSize="15"/>
                            <ComboBox Margin="0,10,0,0" Width="100" Height="50"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Width="330">
                            <TextBlock Text="Установите дату рождения" FontSize="15" VerticalAlignment="Center" Margin="2 2" />
                            <DatePicker Width="80" Margin="30 0"/>
                        </StackPanel>

                    </StackPanel>
                    </Border>
              
            </Border>
        </Grid>
    </Border>
</Page>
